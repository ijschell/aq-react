import React, {Component} from 'react';
import getData from '../services/getData';

const popUp = {
    render(){
        return(
            <div>popup</div>
        )
    }
}

export default popUp;
